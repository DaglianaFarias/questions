

  <table>
  <tbody>
    <p><%= @template.name %></p>

    <% @questions.each do |question| %>
      <tr>
        <td><%= question.question_title %></td>
      </tr>
    <% end %>
  </tbody>
</table>
